# JuliaRatfun { #pyratfun.JuliaRatfun }

```python
JuliaRatfun(julia_obj)
```

Python wrapper for Julia rational functions.

This class wraps Julia's AbstractRationalFunction type, providing a Python
interface for rational function operations including evaluation, arithmetic,
and analysis of poles, zeros, and residues.

Attributes:
    julia: The underlying Julia rational function object.

## Methods

| Name | Description |
| --- | --- |
| [degree](#pyratfun.JuliaRatfun.degree) | Get the total degree of the rational function. |
| [degrees](#pyratfun.JuliaRatfun.degrees) | Get the numerator and denominator degrees. |
| [get](#pyratfun.JuliaRatfun.get) | Get a property from the underlying Julia object. |
| [isempty](#pyratfun.JuliaRatfun.isempty) | Check if the rational function is empty/undefined. |
| [isreal](#pyratfun.JuliaRatfun.isreal) | Check if the rational function has real coefficients. |
| [poles](#pyratfun.JuliaRatfun.poles) | Compute the poles of the rational function. |
| [residues](#pyratfun.JuliaRatfun.residues) | Compute poles and their residues. |
| [roots](#pyratfun.JuliaRatfun.roots) | Compute the zeros of the rational function. |

### degree { #pyratfun.JuliaRatfun.degree }

```python
JuliaRatfun.degree()
```

Get the total degree of the rational function.

Returns:
    int: Maximum of numerator and denominator degrees.

### degrees { #pyratfun.JuliaRatfun.degrees }

```python
JuliaRatfun.degrees()
```

Get the numerator and denominator degrees.

Returns:
    tuple: (numerator_degree, denominator_degree).

### get { #pyratfun.JuliaRatfun.get }

```python
JuliaRatfun.get(field)
```

Get a property from the underlying Julia object.

Args:
    field: Name of the property to retrieve.

Returns:
    The value of the requested property.

### isempty { #pyratfun.JuliaRatfun.isempty }

```python
JuliaRatfun.isempty()
```

Check if the rational function is empty/undefined.

Returns:
    bool: True if the function is empty.

### isreal { #pyratfun.JuliaRatfun.isreal }

```python
JuliaRatfun.isreal()
```

Check if the rational function has real coefficients.

Returns:
    bool: True if all coefficients are real.

### poles { #pyratfun.JuliaRatfun.poles }

```python
JuliaRatfun.poles()
```

Compute the poles of the rational function.

Returns:
    np.ndarray: Array of pole locations in the complex plane.

### residues { #pyratfun.JuliaRatfun.residues }

```python
JuliaRatfun.residues()
```

Compute poles and their residues.

Returns:
    tuple: (poles, residues) as numpy arrays.

### roots { #pyratfun.JuliaRatfun.roots }

```python
JuliaRatfun.roots()
```

Compute the zeros of the rational function.

Returns:
    np.ndarray: Array of zero locations in the complex plane.